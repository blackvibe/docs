---
title: Упоминания контекста
description: Как использовать упоминания (@) в Harvi Code для ссылок на файлы, папки, проблемы, терминал и Git коммиты для более точной работы AI.
---

# Упоминания контекста

Упоминания контекста - это мощный способ дать Harvi Code конкретную информацию о твоем проекте. С их помощью он работает точнее и эффективнее. Ты можешь упоминать файлы, папки, проблемы, вывод терминала и Git коммиты.

Все упоминания начинаются с символа `@`.

---

## Типы упоминаний

| Тип | Формат | Описание | Пример |
|-----|--------|----------|--------|
| **Файл** | `@/path/to/file.ts` | Включает содержимое файла в контекст | "Объясни функцию в @/src/utils.ts" |
| **Изображение** | `@/path/to/image.png` | Включает изображение как визуальный контент | "Что не так с UI? @/screenshots/bug.png" |
| **Папка** | `@/path/to/folder` | Включает содержимое всех файлов в папке | "Проанализируй код в @/src/components" |
| **Проблемы** | `@problems` | Включает диагностику из панели Problems | "@problems Исправь все ошибки" |
| **Терминал** | `@terminal` | Включает последнюю команду и ее вывод | "Исправь ошибки из @terminal" |
| **Git коммит** | `@a1b2c3d` | Ссылается на конкретный коммит по хешу | "Что изменилось в @a1b2c3d?" |
| **Git изменения** | `@git-changes` | Показывает незакоммиченные изменения | "Предложи сообщение для @git-changes" |
| **URL** | `@https://example.com` | Импортирует контент с сайта | "Суммируй @https://docusaurus.io/" |

### Упоминание файлов

| Возможность | Детали |
|-------------|--------|
| **Формат** | `@/path/to/file.ts` (всегда начинай с `/` от корня workspace) |
| **Что дает** | Полное содержимое файла с номерами строк |
| **Поддержка** | Текстовые файлы, PDF и DOCX (с извлечением текста) |
| **Где работает** | В начальных запросах, ответах и последующих сообщениях |
| **Ограничения** | Очень большие файлы могут быть обрезаны; бинарные файлы не поддерживаются |

### Упоминание изображений

Упоминания изображений позволяют включить визуальный контент прямо в разговор. Если модель поддерживает vision, изображение отправляется как визуальный контент.

| Возможность | Детали |
|-------------|--------|
| **Формат** | `@/path/to/image.png` (тот же формат пути, что и для файлов) |
| **Что дает** | Изображение отправляется модели как визуальный контент |
| **Поддержка** | PNG, JPG, JPEG, GIF, BMP, SVG, WEBP, ICO, AVIF |
| **Лучше всего для** | Обзор UI, отладка скриншотов, анализ диаграмм |
| **Требует** | Модель с поддержкой vision (модели без vision не могут интерпретировать изображения) |

### Упоминание папок

| Возможность | Детали |
|-------------|--------|
| **Формат** | `@/path/to/folder` (без слеша в конце) |
| **Что дает** | Полное содержимое всех файлов в директории |
| **Включает** | Содержимое текстовых файлов прямо в папке (не рекурсивно) |
| **Лучше всего для** | Предоставление контекста из нескольких файлов в директории |
| **Совет** | Следи за лимитами контекстного окна при упоминании больших директорий |

### Упоминание проблем

| Возможность | Детали |
|-------------|--------|
| **Формат** | `@problems` |
| **Что дает** | Все ошибки и предупреждения из панели Problems VS Code |
| **Включает** | Пути к файлам, номера строк и сообщения диагностики |
| **Группировка** | Проблемы организованы по файлам для ясности |
| **Лучше всего для** | Исправление ошибок без ручного копирования |

### Упоминание терминала

| Возможность | Детали |
|-------------|--------|
| **Формат** | `@terminal` |
| **Что захватывает** | Последнюю команду и ее полный вывод |
| **Сохраняет** | Состояние терминала (не очищает терминал) |
| **Ограничение** | Ограничено видимым содержимым буфера терминала |
| **Лучше всего для** | Отладка ошибок сборки или анализ вывода команд |

### Git упоминания

| Тип | Формат | Что дает | Ограничения |
|-----|--------|----------|-------------|
| **Коммит** | `@a1b2c3d` | Сообщение коммита, автор, дата и полный diff | Работает только в Git репозиториях |
| **Рабочие изменения** | `@git-changes` | Вывод `git status` и diff незакоммиченных изменений | Работает только в Git репозиториях |

### Упоминание URL

| Возможность | Детали |
|-------------|--------|
| **Формат** | `@https://example.com` |
| **Обработка** | Использует headless браузер для получения контента |
| **Очистка** | Удаляет скрипты, стили и навигационные элементы |
| **Вывод** | Конвертирует контент в Markdown для читаемости |
| **Ограничение** | Сложные страницы могут конвертироваться не идеально |

---

## Как использовать упоминания

1. Напечатай `@` в поле ввода чата - появится выпадающий список подсказок
2. Продолжай печатать для фильтрации или используй стрелки для навигации
3. Выбери нужное через Enter или кликом мыши
4. Комбинируй несколько упоминаний: "Исправь @problems в @/src/component.ts"

Выпадающий список автоматически предлагает:
- Недавно открытые файлы
- Видимые папки
- Недавние git коммиты
- Специальные ключевые слова (`problems`, `terminal`, `git-changes`)
- **Все открытые файлы** (независимо от настроек игнорирования)

По умолчанию список уважает `.rooignore` и скрывает игнорируемые файлы. Включи настройку `showRooIgnoredFiles`, чтобы видеть их (они будут помечены 🔒). Обычные директории типа `node_modules`, `.git`, `dist` и `out` тоже фильтруются.

---

## Важное поведение

### Взаимодействие с игнорируемыми файлами

| Поведение | Описание |
|-----------|----------|
| **Фильтрация в dropdown** | Выпадающий список `@` по умолчанию скрывает файлы из `.rooignore`. Включи `showRooIgnoredFiles`, чтобы видеть их (с меткой 🔒). |
| **Обход `.rooignore`** | Упоминания файлов и папок `@` обходят проверки `.rooignore` при получении контента. Контент из игнорируемых файлов будет включен, если упомянут напрямую. |
| **Обход `.gitignore`** | Аналогично, упоминания файлов и папок не уважают правила `.gitignore` при получении контента. |
| **Уважение Git командами** | Git-упоминания (`@git-changes`, `@commit-hash`) уважают `.gitignore`, так как полагаются на Git команды. |

---

Теперь ты знаешь, как давать Harvi Code точный контекст. Следующий шаг - изучить [режимы работы](/basic-usage/modes), чтобы использовать Harvi Code для разных типов задач.
